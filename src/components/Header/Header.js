import React from 'react';

import logo from '../../images/Logo.svg';

const Header = () => {
    return (
        <nav>
            <img src={logo} alt="" />
        </nav>
    );
};

export default Header;